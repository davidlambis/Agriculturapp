package interedes.agriculturapp.activities.productor;

import android.support.v7.app.AppCompatActivity;
import android.os.Bundle;

import interedes.agriculturapp.R;

public class PreguntasProductorActivity extends AppCompatActivity {

    @Override
    protected void onCreate(Bundle savedInstanceState) {
        super.onCreate(savedInstanceState);
        setContentView(R.layout.activity_preguntas_productor);
    }
}
